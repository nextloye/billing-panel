<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Invoice #<%= invoice.id %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <nav class="bg-white shadow p-4 flex justify-between items-center">
    <h1 class="text-2xl font-bold text-blue-600">Invoice #<%= invoice.id %></h1>
    <a href="/" class="text-gray-700 hover:text-blue-500 font-semibold">Back</a>
  </nav>

  <main class="p-8 max-w-3xl mx-auto bg-white rounded shadow">

    <h2 class="text-xl font-bold mb-4">Payment Instructions</h2>

    <div class="mb-6">
      <h3 class="font-semibold text-gray-700 mb-1">Pay using UPI</h3>
      <p class="text-gray-600">UPI ID: <span class="font-mono"><%= upi %></span></p>
    </div>

    <div class="mb-6">
      <h3 class="font-semibold text-gray-700 mb-1">Or pay with Litecoin (LTC)</h3>
      <p class="text-gray-600">LTC Address: <span class="font-mono"><%= ltc %></span></p>
    </div>

    <div class="mb-6">
      <h3 class="font-semibold text-gray-700 mb-2">Upload Payment Screenshot</h3>
      <form method="POST" action="/invoice/<%= invoice.id %>/upload" enctype="multipart/form-data" class="flex flex-col gap-2">
        <input type="file" name="ss" required class="border p-2 rounded">
        <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded font-semibold">
          Upload
        </button>
      </form>
    </div>

    <p class="font-semibold">Current Status: 
      <% if (invoice.status === "PAID") { %>
        <span class="text-green-600"><%= invoice.status %></span>
      <% } else if (invoice.status === "PENDING") { %>
        <span class="text-yellow-600"><%= invoice.status %></span>
      <% } else { %>
        <span class="text-red-600"><%= invoice.status %></span>
      <% } %>
    </p>

  </main>

</body>
</html>
